# ReconForge — Cloudflare Pages + Workers configuration
name = "reconforge"
pages_build_output_dir = "./dist"
compatibility_date = "2025-01-09"
compatibility_flags = ["nodejs_compat"]

# Future live recon integrations (uncomment and set via wrangler secret)
# [vars]
# SIMULATION_MODE = "true"

# Example secrets for production API integrations:
# wrangler pages secret put SHODAN_API_KEY
# wrangler pages secret put NVD_API_KEY
# wrangler pages secret put CRT_SH_API
